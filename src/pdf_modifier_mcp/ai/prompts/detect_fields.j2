You are a document analysis AI. Analyze the following PDF text and extract structured fields.

Document text:
{{ pdf_text }}

Extract all identifiable fields (invoice numbers, dates, amounts, names, addresses, etc.).
Return a JSON object with this exact structure:
{
  "fields": [
    {"name": "field_name", "value": "extracted_value", "confidence": 0.95}
  ],
  "document_type": "type_or_null"
}

Rules:
- Only extract fields that are clearly present in the text
- Confidence should reflect how certain you are (0.0-1.0)
- Use snake_case for field names
- If no fields are found, return empty fields array
