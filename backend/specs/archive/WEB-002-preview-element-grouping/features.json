[
  {
    "id": "WEB-002-f1",
    "behavior": "Sidebar groups consecutive same-line/same-font/same-size spans into one entry; two-column layout does not merge across columns",
    "verification": "cd frontend && npx vitest run src/lib/utils/grouping.test.ts",
    "state": "pending",
    "evidence": ""
  },
  {
    "id": "WEB-002-f2",
    "behavior": "Clicking a sidebar entry navigates the preview to the element's page and scrolls the container so the highlight is visible (scroll math: computeScrollTarget, zoom-aware via scale; wiring in PdfPreview.svelte + +page.svelte)",
    "verification": "cd frontend && npx vitest run src/lib/utils/scroll.test.ts && npm run check",
    "state": "pending",
    "evidence": ""
  },
  {
    "id": "WEB-002-f3",
    "behavior": "Highlight rects cover only the matched text bounds (proportional within item, multi-item join on same baseline), not the full pdf.js item width",
    "verification": "cd frontend && npx vitest run src/lib/utils/highlight.test.ts",
    "state": "pending",
    "evidence": ""
  },
  {
    "id": "WEB-002-f4",
    "behavior": "Type contract matches backend serialization (elements, not text_elements); svelte-check passes with 0 errors; backend/ untouched",
    "verification": "cd frontend && npm run check && git -C .. diff --name-only master -- backend/ | grep -v specs/WEB-002-preview-element-grouping | grep -q . && exit 1 || exit 0",
    "state": "pending",
    "evidence": ""
  }
]
